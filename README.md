# email_spam_classification_flask
This is an app that can specify any email as spam or ham when trained over half million dataset. It uses Tf-IDF algorithm to extract important features from text and then utilities Naive bates algorithm for prediction. To make it more interesting this has been deployed using Flask app 
